name: sign
on: 
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  first:
    runs-on: ubuntu-latest
    timeout-minutes: 1

    steps:
      - name: first
        run: |
          curl -sSL -X POST 'https://www.duckcoding.ai/api/user/checkin' \
            -H "Cookie: ${{ secrets.DUCK_COOKIE }}" \
            -H "new-api-user: ${{ secrets.DUCK_USER }}" \
            -w "\n响应状态码: %{http_code}\n"
